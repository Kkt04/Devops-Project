on:
  workflow_dispatch:
      inputs:
          name:
            description: "Name"
            default: "Kalash"
            required: true 
          city:
            description: "City"
            default: "Jamshedpur"
            required: true
          country:
            default: "India"
            required: true